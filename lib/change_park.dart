import 'package:flutter/material.dart';

class ChangePark extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Change Park'),),
      body: SafeArea(
        child: null
      ),
    );
  }
}
